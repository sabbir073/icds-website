# Public site URL — used by metadata, sitemap, robots, JSON-LD, OG tags.
# Must be the canonical, fully-qualified production origin without trailing slash.
NEXT_PUBLIC_SITE_URL=https://www.icdsbd.com
